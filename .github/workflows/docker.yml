name: Docker Image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - 'container/**'
      - '.github/workflows/docker.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          # The token-for-token org is being deleted, and ghcr packages do not
          # follow a repo transfer: this repo moved to ffaerber, its package did
          # not. Pushing here kept writing into an org that will not exist.
          images: ghcr.io/ffaerber/t4t
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.source=https://github.com/ffaerber/t4t

      - uses: docker/build-push-action@v6
        with:
          context: ./container
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
